import styled from '~/styled';

export const Button = styled.button`
  // reset button style
  background: none;
  color: inherit;
  border: none;
  padding: 0;
  font: inherit;
  cursor: pointer;
  outline: inherit;
  user-select: none;
`;
